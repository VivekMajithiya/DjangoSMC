from django.contrib import admin
from smc_app.models import ChatData, UserProfileInfo

# Register your models here.

admin.site.register(ChatData)
admin.site.register(UserProfileInfo)
# Register your models here.
